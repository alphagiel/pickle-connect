import 'package:flutter/material.dart';

class AppColors {
  // Primary colors - soft and friendly
  static const primaryGreen = Color(0xFF4CAF50);
  static const lightGreen = Color(0xFFE8F5E8);
  static const darkGreen = Color(0xFF388E3C);

  // Accent colors
  static const accentBlue = Color(0xFF2196F3);
  static const lightBlue = Color(0xFFE3F2FD);
  static const darkBlue = Color(0xFF1976D2);

  // Status colors
  static const warmOrange = Color(0xFFFF9800);
  static const lightOrange = Color(0xFFFFF3E0);
  static const darkOrange = Color(0xFFF57C00);

  static const softPurple = Color(0xFF9C27B0);
  static const lightPurple = Color(0xFFF3E5F5);
  static const darkPurple = Color(0xFF7B1FA2);

  static const successGreen = Color(0xFF4CAF50);
  static const warningOrange = Color(0xFFFF9800);
  static const errorRed = Color(0xFFF44336);

  // Neutral colors
  static const neutralGray = Color(0xFF757575);
  static const lightGray = Color(0xFFF5F5F5);
  static const mediumGray = Color(0xFFBDBDBD);
  static const darkGray = Color(0xFF424242);

  // Background colors
  static const backgroundColor = Color(0xFFFAFAFA);
  static const cardBackground = Colors.white;
  static const surfaceColor = Colors.white;

  // Text colors
  static const primaryText = Color(0xFF212121);
  static const secondaryText = Color(0xFF757575);
  static const onPrimary = Colors.white;
  static const onSurface = Color(0xFF212121);

  // Skill level colors (5-tier system)
  static const beginnerColor = Color(0xFF4CAF50);     // Green - New players
  static const beginnerLight = Color(0xFFE8F5E8);

  static const noviceColor = Color(0xFF8BC34A);        // Light green - Learning
  static const noviceLight = Color(0xFFF1F8E9);

  static const intermediateColor = Color(0xFF2196F3); // Blue - Developing
  static const intermediateLight = Color(0xFFE3F2FD);

  static const advancedColor = Color(0xFF9C27B0);     // Purple - Competitive
  static const advancedLight = Color(0xFFF3E5F5);

  static const expertColor = Color(0xFFFF5722);       // Deep orange - Tournament level
  static const expertLight = Color(0xFFFBE9E7);

  // Status colors for proposals
  static const openStatus = Color(0xFF4CAF50);
  static const openStatusLight = Color(0xFFE8F5E8);
  
  static const acceptedStatus = Color(0xFF2196F3);
  static const acceptedStatusLight = Color(0xFFE3F2FD);

  static const expiredStatus = Color(0xFFFF9800);
  static const expiredStatusLight = Color(0xFFFFF3E0);

  static const completedStatus = Color(0xFF9C27B0);
  static const completedStatusLight = Color(0xFFF3E5F5);

  static const canceledStatus = Color(0xFF757575);
  static const canceledStatusLight = Color(0xFFF5F5F5);
}